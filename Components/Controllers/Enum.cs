public enum UrgencyLevel
{
    None,
    Yellow,
    Orange,
    Red,
    Gray
}

public enum Types { DAILY, WEEKLY, MONTHLY, YEARLY, PERIODICALLY }
public enum Days { FRIDAY, MONDAY, SATURDAY, SUNDAY, THURSDAY, TUESDAY, WEDNESDAY }
